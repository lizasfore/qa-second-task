{
  "devDependencies": {
    "cypress": "^13.3.2",
    "cypress-xpath": "^2.0.1"
  },
  "scripts": {
    "cypress:open": "cypress open",
    "cy:run:second": "cypress run --e2e --browser chrome --config-file cypress.second.config.js"
  }
}
